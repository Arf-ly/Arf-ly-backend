# Local Develop ENV

firebase:
  key-path: ${FIREBASE_LOCAL_KEY_PATH}

spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: Asia/Seoul