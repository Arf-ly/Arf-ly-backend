# Product Develop ENV

firebase:
  key-path: ${FIREBASE_KEY_PATH}


spring:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: Asia/Seoul